import { NominationResponse } from './nomination-response';

describe('NominationResponse', () => {
  it('should create an instance', () => {
    expect(new NominationResponse()).toBeTruthy();
  });
});
